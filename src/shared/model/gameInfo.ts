export class gameInfo {
  totalWords: number;
  lastUpdateDate: string;

  constructor(totalWords: number, lastUpdateDate: string) {
    this.totalWords = totalWords;
    this.lastUpdateDate = lastUpdateDate;
  }
}
